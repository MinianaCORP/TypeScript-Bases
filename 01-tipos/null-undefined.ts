(() => {
  // //! Los valores boleanos anteriormente podían contener valores "null" y "unidefined"
  // //? tsconfig.json -> strictNullChecks -> trabaja para que los boleanos puedan contener "null" y "undefined".

  // let isActive : boolean = undefined;
  // let isRunning : boolean = null;

})()